import { Box } from "@radix-ui/themes";

export default function ContentCard() {
    return(
        <Box className="text-purple-700 bg-white p-8 rounded-2xl">
            <p>300% faster.</p>
        </Box>
    )
}